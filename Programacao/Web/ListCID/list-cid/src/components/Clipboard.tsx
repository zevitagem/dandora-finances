export default function Clipboard() {
    const handleClipboard = () => {
        // Pego o elemento button [Camada de Dados]
        
        // Button dispara um evento com a função Copiar [Camada Evento]

        // Função Copiar [Camada Lógico]
            // pega o valor no campo
    }

    return (
        <>
            <button onClick={handleClipboard}></button>   
        </>
    )
}