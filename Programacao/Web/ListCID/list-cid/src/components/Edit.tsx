export default function Edit() {   
    const handleEdit = () => {
        // Pego o card [Camada de Dados]

        // Verifico se o card existe [Camada Lógica]
        
        // Editar o Card [Camada Lógica]
        
        // Editar o Banco de Dados [Camada Repositório]

    }

    return (
        <>
            <button onClick={handleEdit}></button>   
        </>
    )
}