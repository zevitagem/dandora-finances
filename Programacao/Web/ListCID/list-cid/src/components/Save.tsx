export default function Save() {   
    const handleSave = () => {
        // Pega o card [Camada de Dados]

        // Salvar [Camada Lógica]
        
    }

    return (
        <>
            <button onClick={handleSave}></button>   
        </>
    )
}